

->arrumar as msg de erro
{
    gcesar-n@c1r6p3:~$ echo >>>
    bash: syntax error near unexpected token `>'
}

->testar parse direito  (fzr um .sh?)
{
    aspas (ex: sem fechar, quantidade incorreta, etc)
    variavel is_boom
    operadores (ex: ||, >>>,><)  obs: <> eh valido
    começa ou termina c operador
}

->ver oq faz casos com ';' ou '\'
{
    no pdf: "Not interpret unclosed quotes or special characters which are not required by the
    subject such as \ (backslash) or ; (semicolon)."
}

-implementar garbage_collector()






testar no minishell:

echo 'texto entre aspas simples'
echo "texto entre aspas duplas"
echo $HOME
ls -l | grep txt
cat file.txt > output.txt
echo foo && echo bar
echo foo || echo bar
echo "Mistura de 'aspas' dentro"
echo <> file



testar no bash normal:

echo 'texto entre aspas simples'
echo "texto entre aspas duplas"
echo $HOME
ls -l | grep txt
cat file.txt > output.txt
echo foo && echo bar
echo foo || echo bar
echo "Mistura de 'aspas' dentro"
VAR=teste; echo $VAR
for i in 1 2 3; do echo $i; done
echo <> file
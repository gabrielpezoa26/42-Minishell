

proxs passos:

-> implementar o echo isoladamente
{
    echo "abcd"
    echo -n "abcd"

    printar $USER, $HOME, etc
}

-> começar funçao execution
{
    - lê qual built-in/comando eh e chama
    - vê se deu certo ou n
    - t_command?
}





----------------------------------------

pra depois:

->testar parse direito  (fzr um .sh cpa)
{
    aspas (ex: sem fechar, quantidade incorreta, etc)
    variavel is_boom
    variavel invalida $POTATO
    operadores (ex: ||, >>>,><)  obs: <> eh valido()
    começa ou termina c operador
}



testar no minishell:

echo 'texto entre aspas simples'
echo "texto entre aspas duplas"
echo $HOME
ls -l | grep txt
cat file.txt > output.txt
echo foo && echo bar
echo foo || echo bar
echo "Mistura de 'aspas' dentro"
echo <> file


testar no bash normal:

echo 'texto entre aspas simples'
echo "texto entre aspas duplas"
echo $HOME
ls -l | grep txt
cat file.txt > output.txt
echo foo && echo bar
echo foo || echo bar
echo "Mistura de 'aspas' dentro"
VAR=teste; echo $VAR
for i in 1 2 3; do echo $i; done
echo <> file



n esquecer:
    casos especificos
    {
        operador <> eh valido, no nosso ele invalida
    }